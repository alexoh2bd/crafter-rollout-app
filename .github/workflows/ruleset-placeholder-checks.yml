# Cursor (AI-assisted).

# If your org "Protect" ruleset requires these EXACT check names but no workflow emits them,
# GitHub shows "Expected" forever. Options:
#   (1) Prefer: GitHub → Settings → Rules → edit ruleset → remove checks you don't use.
#   (2) Or: keep this workflow until you replace stubs with real integrations.
#
# After the next PR run, open the PR checks UI and copy the exact check identifiers GitHub shows,
# then align branch protection "required statuses" to those strings (often "Workflow name / Job name").
name: Protect ruleset placeholders

on:
  pull_request:
    branches: [main, master, develop]
  push:
    branches: [main, master, develop]

jobs:
  ai-attribution:
    name: AI Attribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder (replace with real policy or remove from ruleset)
        run: echo "ok"

  large-file-scan:
    name: Large File Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — add git diff size limits or a scanner here
        run: echo "ok"

  pr-summary:
    name: PR Summary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — add LLM/comment bot or remove from ruleset
        run: echo "ok"

  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — prefer GitHub secret scanning or gitleaks in CI
        run: echo "ok"
