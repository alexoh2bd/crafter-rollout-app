"""Rollout storage: JSONL writer and metadata persistence.

Interface defined here; implemented in PR 4 (storage).
Supports local filesystem (dev) and Supabase (prod) via STORAGE_MODE env var.
"""

from __future__ import annotations

# TODO: implement RolloutWriter and MetadataStore (PR 4)
