/**
 * useWebSocket — manages a WebSocket connection with auto-reconnect.
 * Implemented in PR 8.
 */

// TODO: implement in PR 8
export function useWebSocket(_url: string) {
  throw new Error("Not implemented (PR 8)");
}
