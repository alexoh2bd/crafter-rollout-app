/**
 * useGameSession — high-level hook wrapping session lifecycle and WebSocket.
 * Implemented in PR 9.
 */

import type { SessionMode } from "../types";

// TODO: implement in PR 9
export function useGameSession(_mode: SessionMode) {
  throw new Error("Not implemented (PR 9)");
}
